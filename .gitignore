# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/
.zcode/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Sign
/android/root.jks
/android/key.properties

# Scripts
/node_modules

# Output
/artifacts


rust-libs/ease-client-backend/src/controllers/generated.rs
rust-libs/ease-client-shared/src/backends/generated.rs
rust-libs/ease-remote-storage/src/generated.rs

**/target
**/node_modules

# Plugin rspack build output (`plugins/<id>/dist`) — packaged into the
# committed registry (`plugins/registry/`) by `scripts/package-plugin.ts`.
/plugins/*/dist/
# Bundled plugin zip copied into the APK assets by `--bundle` (only
# WebDAV ships there; regenerated from the registry zip on build).
/android/app/src/main/assets/plugin-bundles/

# Playwright MCP session logs
/.playwright-mcp/

.fuse*

# Build cache & local state
.gradle/
/build/
.metadata
.vscode/
